class Example < Formula
  desc "Short description of the command-line tool"
  homepage "https://github.com/OWNER/PROJECT"
  url "https://github.com/OWNER/PROJECT/archive/refs/tags/v1.0.0.tar.gz"
  sha256 "REPLACE_WITH_SHA256"
  license "MIT"

  def install
    # Replace this with the build and installation commands for the project.
    bin.install "example"
  end

  test do
    assert_match version.to_s, shell_output("#{bin}/example --version")
  end
end

